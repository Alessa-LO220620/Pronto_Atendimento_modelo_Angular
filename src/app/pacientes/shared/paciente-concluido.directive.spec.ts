import { PacienteConcluidoDirective } from './paciente-concluido.directive';

describe('PacienteConcluidoDirective', () => {
  it('should create an instance', () => {
    const directive = new PacienteConcluidoDirective();
    expect(directive).toBeTruthy();
  });
});
